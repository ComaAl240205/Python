services:
  db:
    image: postgres:16
    container_name: connected_pg
    environment:
      POSTGRES_USER: connected
      POSTGRES_PASSWORD: connected
      POSTGRES_DB: connected
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: